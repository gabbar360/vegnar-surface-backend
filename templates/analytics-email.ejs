<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Analytics Report</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            background-color: #f5f7fa; 
            width: 100% !important;
            min-width: 100%;
            -webkit-text-size-adjust: 100%;
            -ms-text-size-adjust: 100%;
        }
        .container { 
            max-width: 600px; 
            width: 100%;
            margin: 0 auto; 
            background: white;
        }
        .header { 
            background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%); 
            padding: 20px 15px; 
            text-align: center; 
        }
        .header h1 { 
            color: white; 
            font-size: 20px; 
            margin-bottom: 8px;
            line-height: 1.2;
        }
        .header p { 
            color: rgba(255,255,255,0.9); 
            font-size: 13px;
            line-height: 1.4;
        }
        .content { 
            padding: 20px 15px; 
        }
        .analytics-info { 
            background: #f8fafc; 
            border-radius: 8px; 
            padding: 15px; 
            margin-bottom: 20px; 
        }
        .info-row { 
            display: block;
            margin-bottom: 12px; 
            padding: 8px 0;
            border-bottom: 1px solid #e5e7eb;
        }
        .info-row:last-child { 
            margin-bottom: 0;
            border-bottom: none;
        }
        .info-label { 
            font-weight: 600; 
            color: #374151; 
            font-size: 12px;
            display: block;
            margin-bottom: 4px;
        }
        .info-value { 
            color: #6b7280; 
            font-size: 13px;
            word-wrap: break-word;
            word-break: break-all;
            line-height: 1.4;
        }
        .highlight { 
            background: #e0f2fe; 
            color: #0277bd; 
            padding: 3px 6px; 
            border-radius: 3px; 
            font-weight: 600;
            font-size: 12px;
        }
        .footer { 
            background: #f8fafc; 
            padding: 15px; 
            text-align: center; 
            border-top: 1px solid #e5e7eb; 
        }
        .footer p { 
            color: #6b7280; 
            font-size: 11px;
            line-height: 1.4;
            margin-bottom: 5px;
        }
        .timestamp { 
            background: #e8f5e8; 
            color: #2e7d32; 
            padding: 6px 10px; 
            border-radius: 15px; 
            font-size: 11px; 
            display: inline-block; 
            margin-bottom: 15px;
            text-align: center;
            line-height: 1.3;
        }
        
        /* Desktop styles */
        @media screen and (min-width: 600px) {
            .header { padding: 30px; }
            .header h1 { font-size: 24px; }
            .header p { font-size: 14px; }
            .content { padding: 40px 30px; }
            .analytics-info { padding: 25px; margin-bottom: 25px; }
            .info-row { 
                display: flex;
                align-items: center;
                margin-bottom: 15px;
                padding: 0;
                border-bottom: none;
            }
            .info-label { 
                min-width: 120px; 
                font-size: 14px;
                margin-bottom: 0;
            }
            .info-value { 
                font-size: 14px;
                flex: 1;
                word-break: normal;
            }
            .highlight { 
                padding: 4px 8px;
                font-size: 14px;
            }
            .footer { padding: 25px 30px; }
            .footer p { font-size: 12px; }
            .timestamp { 
                padding: 8px 15px;
                font-size: 12px;
                margin-bottom: 20px;
            }
        }
        
        /* Email client specific fixes */
        @media screen and (max-width: 480px) {
            .container { width: 100% !important; }
            .header h1 { font-size: 18px !important; }
            .content { padding: 15px !important; }
            .analytics-info { padding: 12px !important; }
            .info-value { font-size: 12px !important; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📊 User Analytics Report</h1>
            <p>New user activity detected on your website</p>
        </div>
        
        <div class="content">
            <div class="timestamp">
                🕒 Recorded on <%= new Date().toLocaleString('en-IN', { 
                    timeZone: 'Asia/Kolkata',
                    weekday: 'long', 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit',
                    hour12: true
                }) %>
            </div>
            
            <div class="analytics-info">
                <div class="info-row">
                    <span class="info-label">🆔 Session ID:</span>
                    <span class="info-value"><%= session_id %></span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">✅ Cookie Consent:</span>
                    <span class="info-value"><span class="highlight"><%= consent_type %></span></span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">🌍 Location:</span>
                    <span class="info-value"><%= city %>, <%= country %> (<%= country_code %>)</span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">📍 IP Address:</span>
                    <span class="info-value"><%= ip_address || 'Not detected' %></span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">📄 Pages Visited:</span>
                    <span class="info-value"><%= total_pages || 1 %></span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">⏱️ Session Duration:</span>
                    <span class="info-value"><%= session_duration || 0 %> minutes</span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">🖥️ Browser:</span>
                    <span class="info-value"><%= user_agent %></span>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>📎 Complete Excel report with ALL user analytics is attached.</p>
            <p style="margin-top: 8px;">This includes all website visitors data in one consolidated file.</p>
        </div>
    </div>
</body>
</html>