<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Contact Form Submission</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #f5f7fa; }
        .container { max-width: 600px; margin: 0 auto; background: white; }
        .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 30px; text-align: center; }
        .header h1 { color: white; font-size: 24px; margin-bottom: 8px; }
        .header p { color: rgba(255,255,255,0.9); font-size: 14px; }
        .content { padding: 40px 30px; }
        .contact-info { background: #f8fafc; border-radius: 12px; padding: 25px; margin-bottom: 25px; }
        .info-row { display: flex; margin-bottom: 15px; align-items: center; }
        .info-row:last-child { margin-bottom: 0; }
        .info-label { font-weight: 600; color: #374151; min-width: 100px; font-size: 14px; }
        .info-value { color: #6b7280; font-size: 14px; flex: 1; }
        .message-section { margin-top: 25px; }
        .message-title { font-weight: 600; color: #374151; margin-bottom: 12px; font-size: 16px; }
        .message-content { background: #f0f9ff; border-left: 4px solid #0ea5e9; padding: 20px; border-radius: 8px; color: #0c4a6e; line-height: 1.6; }
        .footer { background: #f8fafc; padding: 25px 30px; text-align: center; border-top: 1px solid #e5e7eb; }
        .footer p { color: #6b7280; font-size: 12px; }
        .timestamp { background: #fef3c7; color: #92400e; padding: 8px 15px; border-radius: 20px; font-size: 12px; display: inline-block; margin-bottom: 20px; }
        .icon { width: 16px; height: 16px; margin-right: 8px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📧 New Contact Form Submission</h1>
            <p>You have received a new message from your website</p>
        </div>
        
        <div class="content">
            <div class="timestamp">
                🕒 Received on <%= new Date().toLocaleString('en-US', { 
                    weekday: 'long', 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                }) %>
            </div>
            
            <div class="contact-info">
                <div class="info-row">
                    <span class="info-label">👤 Name:</span>
                    <span class="info-value"><strong><%= full_name %></strong></span>
                </div>
                
                <div class="info-row">
                    <span class="info-label">📧 Email:</span>
                    <span class="info-value"><a href="mailto:<%= email %>" style="color: #0ea5e9; text-decoration: none;"><%= email %></a></span>
                </div>
                
                <% if (phone_number) { %>
                <div class="info-row">
                    <span class="info-label">📱 Phone:</span>
                    <span class="info-value"><a href="tel:<%= phone_number %>" style="color: #0ea5e9; text-decoration: none;"><%= phone_number %></a></span>
                </div>
                <% } %>
                
                <% if (company_name) { %>
                <div class="info-row">
                    <span class="info-label">🏢 Company:</span>
                    <span class="info-value"><%= company_name %></span>
                </div>
                <% } %>
            </div>
            
            <div class="message-section">
                <div class="message-title">💬 Message:</div>
                <div class="message-content">
                    <%= message %>
                </div>
            </div>
        </div>
        
        <div class="footer">
            <p>This email was automatically generated from your website contact form.</p>
            <p style="margin-top: 8px;">Please respond to the customer directly at their email address.</p>
        </div>
    </div>
</body>
</html>